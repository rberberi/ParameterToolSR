<Window x:Class="PTSRDesktopUI.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PTSRDesktopUI.Views"
        mc:Ignorable="d" FontSize="18" 
        Title="Parameter Tool" Height="800" Width="800" Background="#FFE0DEC4" WindowStartupLocation="CenterScreen" WindowState="Maximized">

    <DockPanel>
        <Menu DockPanel.Dock="Top" FontSize="18" Background="#FFD6C8A0">
            <MenuItem Header="_Benutzer">
                <MenuItem x:Name="LogOut" Header="_Ausloggen" Background="#FFD6C8A0" />
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel x:Name="Menu" Grid.Column="0" Margin="5 5 5 5"
                        Visibility="{Binding IsMenuVisible, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                <Button Content="Anlage" Padding="10"/>
                <Button Content="Anlage" Padding="10"/>
                <Button Content="Anlage" Padding="10"/>
                <Button Content="Anlage" Padding="10"/>
                <Button Content="Anlage" Padding="10"/>
                <Button Content="Anlage" Padding="10"/>
                <Button Content="Anlage" Padding="10"/>
                <Button Content="Anlage" Padding="10"/>
                <Button Content="Anlage" Padding="10"/>
            </StackPanel>
            <ContentControl x:Name="ActiveItem" Margin="5" Grid.Column="1" />
        </Grid>
    </DockPanel>

</Window>